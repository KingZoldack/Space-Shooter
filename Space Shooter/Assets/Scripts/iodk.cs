using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class iodk : MonoBehaviour
{
    int counter; 

    // Update is called once per frame
    void Update()
    {
        for(int i =11; i <= 11; i--)
        {   
            //counter++;
            Debug.Log(i);
        }
        //Debug.Log(counter + "This is the counter");
    }
}
